mixin navbar(currentUrl, List)
    ul.navbar-nav.mt-2.mt-lg-0.w-100

        if List
            each item in List
                li.nav-item(class= {active: currentUrl === item.value})
                    a.nav-link(href= item.url)= item.title
